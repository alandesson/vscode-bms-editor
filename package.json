{
  "name": "bms-renderer",
  "publisher": "AlandessonLinhares",
  "icon": "resources/bms-logo.png",
  "displayName": "BMS Renderer",
  "description": "BMS renderer and language support ",
  "version": "1.0.2",
  "engines": {
    "vscode": "^1.109.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alandesson/vscode-bms-editor/"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "configuration": {
      "title": "BMS Renderer",
      "properties": {
        "bms-renderer.suppressConflictWarning": {
          "type": "boolean",
          "default": false,
          "description": "Suppress the warning shown when a conflicting mainframe extension (e.g. IBM Z Open Editor) is detected. The warning explains how to restore proper syntax highlighting and renderer access."
        }
      }
    },
    "menus": {
      "editor/title": [
        {
          "command": "bms-renderer.render",
          "when": "resourceLangId == bms || resourceExtname == .bms || resourceExtname == .bbms",
          "group": "navigation"
        }
      ],
      "explorer/context": [
        {
          "command": "bms-renderer.render",
          "when": "resourceExtname == .bms || resourceExtname == .bbms",
          "group": "navigation"
        }
      ],
      "editor/context": [
        {
          "command": "bms-renderer.render",
          "when": "resourceLangId == bms || resourceExtname == .bms || resourceExtname == .bbms",
          "group": "navigation"
        }
      ]
    },
    "commands": [
      {
        "command": "bms-renderer.render",
        "title": "Open BMS Renderer",
        "icon": "$(eye)"
      }
    ],
    "languages": [
      {
        "id": "bms",
        "aliases": ["BMS", "bms"],
        "extensions": [".bms", ".bbms"],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "bms",
        "scopeName": "source.bms",
        "path": "./syntaxes/bms.tmLanguage.json"
      }
    ],
    "semanticTokenScopes": [
      {
        "language": "bms",
        "scopes": {
          "label.italic": ["support.function.label.bms"],
          "continuation": ["markup.bold.bms"]
        }
      }
    ],
    "configurationDefaults": {
      "files.associations": {
        "*.bbms": "bms"
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "webpack",
    "watch": "webpack --watch",
    "package": "webpack --mode production --devtool hidden-source-map",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.109.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "typescript-eslint": "^8.54.0",
    "eslint": "^9.39.2",
    "typescript": "^5.9.3",
    "ts-loader": "^9.5.4",
    "webpack": "^5.104.1",
    "webpack-cli": "^6.0.1",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}
